// 1. Criando as variáveis de nome e sobrenome
const nome = "João";
const sobrenome = "Silva";

// 2. Gerando a terceira variável nomeCompleto concatenando as anteriores

const nomeCompleto = `${nome} ${sobrenome}`;

// 3. Exibindo o resultado no console
console.log(nomeCompleto); // Saída: "João Silva"
