// 1. Definição dos valores
let base = 2;
let x = 3;

// 2. Cálculo e armazenamento (base elevado a x)
let resultado = base ** x;

// Exibição
console.log(`${base} elevado a ${x} é: ${resultado}`); // Saída: 8
