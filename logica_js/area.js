// 1. Armazenar a base e a altura
let base = 10;
let altura = 5;

// 2. Calcular a área usando a fórmula (base * altura) / 2
let area = (base * altura) / 2;

// 3. Exibir o resultado
console.log("Base: " + base);
console.log("Altura: " + altura);
console.log("A área do triângulo é: " + area); // Resultado: 25
